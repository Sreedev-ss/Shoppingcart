<style>
    .dropdown-menu{
        min-width: 6.5rem;
        background-color: rgba(255, 255, 255, 0.365);
        padding: 0px;
     
    }
</style>
<header>
    <nav class="navbar navbar-expand-lg bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="/"><b>Shopping Cart</b></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse text-end" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    <li class="nav-item">
                        <a class="nav-link text-white " aria-current="page" href="/">Products</a>
                    </li>
                    {{#if user}}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/">Cart</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/">Your Orders</a>
                    </li>
                    {{/if}}
                </ul>
                <div class="dropdown ">
                    <button class="btn btn-light dropdown-toggle" type="button" id="dropdownMenuButton1"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        {{#if user}}
                        Sreedev
                        {{else}}
                        Accounts
                        {{/if}}
                    </button>
                    <ul class="dropdown-menu mt-2 " aria-labelledby="dropdownMenuButton1">
                        {{#if user}}
                        <li><a class="dropdown-item hoi text-center justify-content-end" href="/logout">Logout</a></li>
                        {{else}}
                        <li><a class="dropdown-item text-center" href="/login">Login</a></li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </div>
    </nav>
</header>